import React from 'react';

const Tile = (props) => {
    return(
        <div className="tile" onClick={props.onClick}>
            <p className="letter">
                {props.value}
            </p>
        </div>
    )
}

export default Tile;